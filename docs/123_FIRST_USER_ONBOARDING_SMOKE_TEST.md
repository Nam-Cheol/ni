# First-User Onboarding Smoke Test

## Current status

State:

- v0.5.0 publication: verified
- current-tree README two-path onboarding: implemented
- ni init . guided onboarding: implemented
- macOS current-tree smoke: verified in this task
- Windows real-host execution: deferred on macOS-only development host
- Homebrew: Planned / v0.5 candidate
- Model workspace packs: Experimental
- No-terminal method: Experimental / assisted
- Skills are UX; CLI is authority.
- ni is a pre-runtime Project Intent Compiler for AI Agents.

## Smoke goal

This smoke test checks whether a first user can follow the README-shaped path
to a usable planning workspace from the current repository tree. It is not a
release publication check, not a Windows real-host transcript, and not
downstream execution proof.

## Smoke lanes

| Lane | Purpose | Binary source | Expected version | Result | Notes |
| --- | --- | --- | --- | --- | --- |
| current-tree onboarding smoke | Verify README first-user command-name onboarding from this checkout. | Local `go build` output copied into a temporary user bin directory. | Current tree output; observed `0.0.0-dev`. | Verified on macOS. | Used temporary `HOME`, `PATH`, and project directories. |
| published v0.5.0 install smoke | Verify released installer or release artifact behavior. | Published GitHub Release assets or curl installer. | `0.5.0`. | Not run in this task. | `install.sh --dry-run --version 0.5.0` was run only to inspect asset selection; no release asset was downloaded or installed in this task. |
| Windows real-host smoke | Verify PowerShell install, new-session command resolution, help/version, and uninstall on Windows. | `install.ps1` on a real Windows host, VM, CI runner, or external tester. | `0.5.0` for the published lane, or current-tree version for a current-tree Windows lane. | Deferred. | Static Windows installer safety checks passed; real-host execution is not claimed. |

## macOS current-tree transcript

| Step | Command | Expected | Observed | Pass? | Notes |
| --- | --- | --- | --- | --- | --- |
| build current tree | `GOCACHE=/private/tmp/ni-go-cache go build -o /private/tmp/ni-task202-bin/ni ./cmd/ni` | Local binary builds. | Built without output. | Yes | This is Lane A, not the published v0.5.0 binary. |
| fresh shell command lookup | `command -v ni` | Resolves by command name. | `/private/tmp/ni-task202-smoke/bin/ni` | Yes | Ran inside `/bin/zsh -f` with temporary `HOME` and `PATH`. |
| help | `ni --help` | Help succeeds without absolute path. | Output contained `ni is a project intent compiler.` | Yes | Full output captured in `/private/tmp/ni-task202-smoke/help.txt`. |
| version | `ni version` | Version succeeds without absolute path. | `0.0.0-dev` | Yes | Current-tree build behavior. |
| project directory | `mkdir first-user-project && cd first-user-project` | Fresh project directory exists. | Created under `/private/tmp/ni-task202-smoke/projects`. | Yes | No project-root files used. |
| init | `ni init . --yes` | Init creates planning artifacts and no lockfile. | Created `.ni/contract.json`, `.ni/session.json`, and 12 `docs/plan` files. | Yes | `--yes` is the supported unattended path. |
| status | `ni status --proof --next-questions` | Readiness gate runs after init. | Command succeeded and reported `NI Intent Readiness: BLOCKED`. | Yes | Blocking result is expected for a minimal first-run workspace with missing intent. |
| interactive stdin | `printf ... | ni init . --interactive` | Interactive guided init works from stdin. | Created `.ni/contract.json` and 12 `docs/plan` files. | Yes | Separate temporary fixture under `/private/tmp/ni-task202-safety`. |

## Generated artifact check

| Artifact | Expected | Observed | Pass? | Notes |
| --- | --- | --- | --- | --- |
| `.ni/contract.json` | Exists after `ni init .`. | Exists. | Yes | Temporary first-user project only. |
| `.ni/session.json` | Exists after `ni init .`. | Exists. | Yes | Temporary first-user project only. |
| `docs/plan/**` | Exists after `ni init .`. | 12 files. | Yes | Includes `docs/plan/00_project_brief.md` through `11_decision_log.md`. |
| `.ni/plan.lock.json` | Must not be created by init. | Absent. | Yes | No lock command was run. |
| downstream generated prompt | Must not be executed or generated by init/status. | `.ni/generated` absent. | Yes | No `ni run` was run in the temporary project. |

## Existing project safety

| Check | Expected | Observed | Pass? | Notes |
| --- | --- | --- | --- | --- |
| first init | Creates planning files. | Created expected planning files. | Yes | Temporary project `/private/tmp/ni-task202-safety/projects/repeat-project`. |
| second init | Does not silently overwrite existing files. | Printed `existing ni planning files found; ni init will not overwrite them.` and `adding missing files only.` | Yes | `.ni/contract.json` and `.ni/session.json` SHA-256 values were unchanged. |

## Lockfile safety

| Check | Expected | Observed | Pass? | Notes |
| --- | --- | --- | --- | --- |
| locked project init | Existing `.ni/plan.lock.json` is not modified. | Printed lockfile warning and `No files changed by ni init; the lockfile was not modified.` | Yes | Sentinel lockfile SHA-256 was unchanged. |
| root lockfile | Project-root `.ni/plan.lock.json` is not touched. | `git diff -- .ni/contract.json .ni/session.json .ni/plan.lock.json` produced no output. | Yes | No project-root `ni end` or relock was run. |

## Installer smoke

| Check | Expected | Observed | Pass? | Notes |
| --- | --- | --- | --- | --- |
| `install.sh --dry-run --version 0.5.0` | Selects the expected release asset without installing. | Selected `ni_0.5.0_darwin_arm64.tar.gz` and printed the v0.5.0 download URLs. | Yes | Dry-run only; not a public v0.5.0 install smoke. |
| `scripts/test-install-sh.sh` | Local fake release fixture tests dry-run, checksum, install, PATH resolution, and uninstall. | Passed through `GOCACHE=/private/tmp/ni-go-cache bash scripts/install-check.sh`. | Yes | This validates installer behavior without network access. |
| uninstall scope | Removes installed binary and only ni-managed PATH block. | Passed through `scripts/test-install-sh.sh` inside install-check. | Yes | The script verified binary removal and ni-managed PATH block removal. |

## Windows status

| Area | Status | Evidence | Notes |
| --- | --- | --- | --- |
| Windows installer implementation | Present. | `install.ps1` exists. | Installs to `%LOCALAPPDATA%\ni\bin` and updates User PATH only. |
| Windows static safety checks | Passed. | `python3 scripts/check-install-ps1.py` printed `Windows installer static safety checks passed`. | Static proof only. |
| Windows real-host execution | Deferred. | No Windows host, VM, CI runner, or external tester transcript was used. | Do not claim Windows global install is verified. |

## README alignment

| README claim | Smoke evidence | Pass? | Notes |
| --- | --- | --- | --- |
| macOS path starts with installer inspection, dry-run, install, new shell, `ni --help`, and `ni version`. | README has the path; current-tree smoke separately verified command-name help/version from a fresh shell context. | Yes | README public installer path remains release-lane language. |
| First project uses `mkdir`, `cd`, `ni init .`, `ni status --proof --next-questions`, `ni end`, and `ni run --max-chars 4000`. | README has the full "First project in 5 minutes" flow. | Yes | This task did not run `ni end` or `ni run` in the project root. |
| Windows path uses PowerShell installer and a new PowerShell session. | Static installer checks passed; real-host execution deferred. | Yes | README keeps the deferred Windows claim. |
| `ni run` compiles but does not execute downstream work. | README states this; smoke did not run generated prompts. | Yes | Runtime execution boundary preserved. |

## Claim-boundary audit

| Claim area | Expected boundary | Observed state | Pass? | Notes |
| --- | --- | --- | --- | --- |
| macOS global install | Current-tree smoke must prove command-name resolution without absolute path. | Temporary `PATH` resolved `ni` and ran help/version. | Yes | This proves the tested local binary lane, not every shell configuration. |
| Windows global install | No verified claim without a Windows transcript. | Static safety check only; real-host execution deferred. | Yes | No Windows verified claim added. |
| Homebrew | Homebrew remains Planned / v0.5 candidate. | README/docs keep Planned wording. | Yes | No Homebrew Available claim. |
| ni init . | Init creates planning artifacts only. | Temporary projects created docs and `.ni` planning files. | Yes | No lockfile or generated prompt created by init. |
| ni status | CLI readiness gate is authority. | Status ran and reported `BLOCKED` for missing first-run intent. | Yes | Model judgment did not declare readiness. |
| ni end | Lock command must not be run on the project root in this task. | Not run on project root. | Yes | No root relock. |
| ni run | Prompt compiler only; no execution. | Not run in temporary smoke or project root. | Yes | No generated prompt executed. |
| READY | CLI-only readiness, not product readiness. | First-run temp project remained `BLOCKED`. | Yes | Expected until blockers are answered. |
| Runtime execution boundary | No task runner, execution harness, shell adapter, Codex exec adapter, queue, PR automation, release automation, or downstream execution layer. | No runtime behavior added. | Yes | Docs-only Task 202 pass. |

## Git status / inclusion check

| Path or group | git status --short | Expected in next commit? | Notes |
| --- | --- | --- | --- |
| README.md | `M` | Existing change; no new Task 202 edit expected. | Pre-existing README visual/onboarding change was present before this task. |
| README.ko.md | `M` | Existing change; no new Task 202 edit expected. | Korean companion was already modified before this task. |
| cmd/ni/* | unchanged in this task | No | Read and built only. |
| internal/* | unchanged in this task | No | Read and tested only. |
| docs/121* | unchanged in this task | No | Used as prior onboarding evidence. |
| docs/123* | `??` | Yes | New smoke-test documentation and Korean companion. |
| install.sh | unchanged | No | Dry-run/read only. |
| install.ps1 | unchanged | No | Static safety check/read only. |
| scripts/* | unchanged in this task | No | Validation only. |
| temporary smoke directories | not tracked | No | Used under `/private/tmp/ni-task202-*`. |
| `.ni/contract.json` | unchanged | No | Protected project-root planning state. |
| `.ni/session.json` | unchanged | No | Protected project-root planning state. |
| `.ni/plan.lock.json` | unchanged | No | Protected project-root lockfile. |
| unexpected files | none from Task 202 observed so far | No | Recheck after final validation. |

## Validation results

| Command | Result |
| --- | --- |
| `git status --short` | Passed; final status shows pre-existing README/docs/asset changes plus new `docs/123*` files. |
| `gofmt -w .` | Not run separately; no Go files were touched by Task 202, and `quality.sh` completed successfully. |
| `GOCACHE=/private/tmp/ni-go-cache go build -o /private/tmp/ni-task202-bin/ni ./cmd/ni` | Passed. |
| current-tree first-user smoke in temporary `HOME` and project directory | Passed. |
| command-name `ni --help` from fresh shell context | Passed. |
| command-name `ni version` from fresh shell context | Passed; `0.0.0-dev`. |
| `ni init . --yes` in temporary project | Passed. |
| `ni status --proof --next-questions` in temporary project | Passed; reported `BLOCKED` with first-run next questions. |
| repeated `ni init .` safety smoke | Passed; contract/session hashes unchanged. |
| lockfile safety smoke | Passed; sentinel `.ni/plan.lock.json` hash unchanged. |
| interactive stdin smoke | Passed. |
| `python3 scripts/check-install-ps1.py` | Passed. |
| `sh install.sh --dry-run --version 0.5.0` | Passed; dry-run only. |
| `GOCACHE=/private/tmp/ni-go-cache go test ./...` | Passed. |
| `GOCACHE=/private/tmp/ni-go-cache go run ./cmd/ni status --dir . --proof --next-questions` | Passed; project root reports `NI Intent Readiness: READY` with no blockers, deferrals, or warnings. |
| `GOCACHE=/private/tmp/ni-go-cache go run ./cmd/ni --help` | Passed. |
| `GOCACHE=/private/tmp/ni-go-cache go run ./cmd/ni version` | Passed; printed `0.0.0-dev`. |
| `python3 scripts/check-install-docs.py` | Passed. |
| `bash scripts/check-skill-packs.sh` | Passed. |
| `bash scripts/demo-check.sh` | Passed; fixture prompt compilation stayed separate from project-root execution. |
| `GOCACHE=/private/tmp/ni-go-cache bash scripts/quality.sh` | Passed. |
| `GOCACHE=/private/tmp/ni-go-cache bash scripts/smoke.sh` | Passed; fixture `ni end`/`ni run` paths stayed separate from project-root lock state. |
| `GOCACHE=/private/tmp/ni-go-cache bash scripts/install-check.sh` | Passed; source, build, temporary global command, installer, uninstall, and Windows static checks passed. |
| `GOCACHE=/private/tmp/ni-go-cache bash scripts/release-check.sh` | Passed. |
| `git diff -- .ni/contract.json .ni/session.json .ni/plan.lock.json` | Passed; no output. |

## Changes made

- Added this Task 202 first-user onboarding smoke-test record.
- Added a Korean companion record with matching claim boundaries.
- No code changes were made for Task 202 because the smoke did not reveal a
  current-tree onboarding blocker.

## What this smoke proves

- current-tree macOS first-user command-name onboarding works for the tested
  path;
- `ni init .` creates expected planning artifacts in a temporary project;
- `ni status --proof --next-questions` runs after init;
- repeated init and lockfile safety behavior are bounded in temporary fixtures;
- project-root `.ni` files were not modified.

## What this smoke does not prove

- Windows real-host execution works;
- Homebrew is Available;
- every shell configuration works;
- external users succeed;
- downstream execution succeeds;
- no-terminal is deterministic;
- the published v0.5.0 artifact includes current-tree `ni init .` behavior
  unless verified from that artifact.

## Recommended next task

Selected: B. external user validation plan

This is selected because the current-tree macOS first-user smoke passed and the
next proof gap is whether an external user can reproduce the README path on a
clean machine without repository-local context.

## Next task prompt

```text
Proceed in /Users/namba/Documents/project/ni.

Task: External first-user onboarding validation plan.

Goal:
Create a claim-safe plan for an external user or tester to validate the README
first-user path without relying on repository-local assumptions.

Read:
- AGENTS.md
- README.md
- README.ko.md
- docs/22_INSTALL.md
- docs/install-curl.md
- docs/install-curl.ko.md
- docs/123_FIRST_USER_ONBOARDING_SMOKE_TEST.md
- docs/123_FIRST_USER_ONBOARDING_SMOKE_TEST.ko.md
- install.sh
- install.ps1
- scripts/check-install-docs.py

Rules:
- Do not publish, tag, release, upload, or push.
- Do not run ni end on the project root.
- Do not relock the project root.
- Do not edit .ni/contract.json, .ni/session.json, or .ni/plan.lock.json.
- Do not execute generated prompts.
- Do not claim Windows real-host verification without a Windows transcript.
- Do not mark Homebrew Available.
- Do not imply ni run executes downstream work.

Work:
1. Define a short external tester script for macOS that records shell, PATH,
   installer command, new-shell command-name checks, ni init ., and
   ni status --proof --next-questions.
2. Define the exact evidence transcript required before claiming external
   success.
3. Keep current-tree and published v0.5.0 release lanes separate.
4. Keep Windows as deferred unless a real Windows host transcript is supplied.
5. Add a concise docs note and Korean companion if the repo maintains companion
   docs.

Validation:
- git status --short
- python3 scripts/check-install-docs.py
- python3 scripts/check-install-ps1.py
- bash scripts/check-skill-packs.sh
- bash scripts/demo-check.sh
- GOCACHE=/private/tmp/ni-go-cache go test ./...
- GOCACHE=/private/tmp/ni-go-cache bash scripts/quality.sh
- git diff -- .ni/contract.json .ni/session.json .ni/plan.lock.json

Final response:
Report changed files, selected validation lane, exact external evidence needed,
validation results, protected .ni diff, and confirmation that no release action,
project-root relock, generated prompt execution, Homebrew Available claim, or
Windows verified claim was added.
```
