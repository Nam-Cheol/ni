# Vague Request: Partner Sync CLI

Category: API or CLI tool

Build a small CLI that syncs partner records with our system. It should be
simple enough for ops to run when needed and should avoid duplicate records.
