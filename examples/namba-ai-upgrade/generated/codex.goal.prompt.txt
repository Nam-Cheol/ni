Codex target prompt

Goal: Paste this prompt into Codex to make the smallest implementation move allowed by the locked NI plan.

Project: namba-ai upgrade planning - Create a locked planning contract for upgrading namba-ai so downstream implementation can start from explicit limitations, boundaries, targets, risks, and evaluations without running or modifying namba-ai.
Readiness: READY
Target: codex (prompt)
Locked at: 2026-05-26T11:54:32Z

Authoritative sources:
- .ni/plan.lock.json is authoritative for lock state, hashes, and source-of-truth order.
- .ni/contract.json carries accepted CAP/REQ/EVAL/RISK IDs and acceptance criteria.
- docs/plan/ contains locked planning context; use only when hashes match.
Source of truth: .ni/plan.lock.json > .ni/contract.json > docs/plan/** > chat transcript > model guess

Rules:
- Treat .ni/plan.lock.json as authoritative over .ni/contract.json, docs/plan, chat, and guesses.
- Verify locked file hashes before using planning docs; on lock mismatch report BLOCKED and stop.
- If there are conflicting requirements, report BLOCKED with conflicting sources or IDs and stop.
- Do not weaken acceptance criteria, risk mitigations, or blocker handling to proceed.
- Every work item must trace to CAP/REQ/EVAL/RISK IDs.
- Keep ni at the pre-runtime boundary; this prompt is seed material, not kernel-owned execution state.
- Do not make ni run execute shell, Codex, adapters, queues, PR automation, or agent teams.

Target instructions:
- Use the current worktree as evidence; preserve user changes and stay within the locked contract.
- Define validation before editing, then implement only the selected work packet.
- Do not ask ni to invoke Codex automatically; ni run only compiled this prompt.

Process:
1. Read .ni/plan.lock.json, .ni/contract.json, and relevant docs/plan files.
2. Choose the smallest coherent packet traced to CAP/REQ/EVAL/RISK IDs.
3. Edit code or docs only as needed, run validation, and report evidence.

Accepted capabilities:
- CAP-001: Capture current namba-ai upgrade limitations as planning evidence
- CAP-002: Model SDD sequencing and collaboration issues as graph-oriented planning constraints
- CAP-003: Preserve a Codex-limited, pre-runtime planning compiler boundary
- CAP-004: Define downstream targets and validation expectations without execution tasks

Accepted requirements:
- REQ-001: The plan must state namba-ai's current upgrade-planning limitations: implicit runtime coupling, vague acceptance traceability, sequence pressure, and unclear evidence ownership.
- REQ-002: The plan must describe the SDD sequence and collaboration problem as dependency graph planning, not a mandatory linear SPEC chain.
- REQ-003: The plan must define a Codex-limited boundary: Codex may consume a compiled prompt, but NI must not invoke Codex or depend on Codex execution state.
- REQ-004: The plan must position NI as a pre-runtime planning compiler that locks intent before namba-ai or any downstream harness receives seed material.
- REQ-005: The plan must name downstream targets including Codex prompt handoff, namba-ai seed guidance, human-team review, and generated harness proposals.
- REQ-006: The generated Codex prompt must be saved under examples/namba-ai-upgrade/generated/ and remain 4000 characters or less.

Risks:
- RISK-001 (high): Treat this example as upgrade planning only; require downstream implementers to verify namba-ai repository facts before changing namba-ai.
- RISK-002 (high): Require graph and dependency language, conflict checks, and traceability to CAP/REQ/EVAL/RISK IDs before implementation packets are selected.
- RISK-003 (high): State that ni run compiles prompt text only; any Codex use is downstream, manual, and outside NI-owned state.
- RISK-004 (high): Keep non-goals explicit and reject shell adapters, queues, SPEC runners, and execution task creation in this example.
- RISK-005 (medium): Require generated material to point back to .ni/plan.lock.json a
[truncated to max-chars]
