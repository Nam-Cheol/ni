# ni next questions

The authoritative next-question group from `ni status --proof
--next-questions` is:

```text
Open blockers:
1. OQ-001: OQ-001 is blocking readiness. Should it be resolved, deferred with reason, or kept blocking with the missing information named?
   Answer shape: accepted decision, deferral with reason, not_applicable, or keep blocking with reason
2. OQ-002: OQ-002 is blocking readiness. Should it be resolved, deferred with reason, or kept blocking with the missing information named?
   Answer shape: accepted decision, deferral with reason, not_applicable, or keep blocking with reason
3. OQ-003: OQ-003 is blocking readiness. Should it be resolved, deferred with reason, or kept blocking with the missing information named?
   Answer shape: accepted decision, deferral with reason, not_applicable, or keep blocking with reason

2 additional lower-priority question(s) remain after these top 3.
```

The checked-in workspace names the five blocker questions as:

- `OQ-001`: What research question, supported decision, and final study
  artifact should the neighborhood cooling study produce?
- `OQ-002`: Which participant or observation scope, inclusion and exclusion
  criteria, locations, and observation windows are accepted?
- `OQ-003`: What consent, privacy, data handling, translation, accessibility,
  and sensitive-data boundaries apply?
- `OQ-004`: What field-team safety rules, heat or weather stop rules,
  vulnerable-group safeguards, and escalation path are required?
- `OQ-005`: Who reviews the protocol, what acceptance evidence is required, and
  what must be ready before fieldwork can be considered?

These questions must be answered, deferred with explicit rationale, marked not
applicable, or kept blocking with reason before lock can proceed.
