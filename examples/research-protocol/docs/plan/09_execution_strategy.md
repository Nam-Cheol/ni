# Execution strategy

## v0 execution strategy

Do not execute implementation automatically. Use `ni run` to compile a short prompt after the plan is locked.

Downstream research work may create protocol review packets, sampling worksheets, or analysis templates. Those are outside ni-kernel and must not become kernel-owned runtime state.
